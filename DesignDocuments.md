---

# 智能资产配置双模推荐系统联合设计文档 (V1.0)

## 一、项目概述

本项目旨在设计并研发一个面向财富管理场景的**智能资产配置前端工作台**。系统创新性地采用“双页签”架构，在一套统一的前台基座上，无缝融合“生成式推荐（宏观、深度的资产配置静态长报告）”**与**“交互式推荐（微观、敏捷的动态流式对话助手）”两种服务模式。
项目以前端极致交互体验为核心突破口，在后端建立现有 `onerec` 推荐引擎与大语言模型（LLM）的桥梁，最终交付一个专业、美观、安全且具备高度可扩展性的金融AI应用。

---

## 二、详细需求设计

### 2.1 整体页面结构

* **全局导航栏 (Global Header)**：包含产品Logo、当前登录用户身份、全局设置以及合规免责声明入口。
* **双模切换中枢 (Segmented Tabs)**：在页面核心区域提供“生成式推荐”与“交互式推荐”大尺寸页签切换。
* *关键交互约束*：引入**页面级 Keep-Alive（状态保活机制）**。用户在生成长报告期间切换至交互助手，再切回时，报告的生成进度与表单状态绝对不能丢失。


* **响应式适配策略**：
* PC端：采用宽幅分栏布局（左侧表单/交互，右侧图表/对比台），最大化展现金融数据表格。
* 移动/Pad端：主推单列沉浸式布局，全面应用 CSS `env(safe-area-inset)` 适配刘海屏及底部手势黑条，彻底解决软键盘遮挡问题。



### 2.2 生成式推荐模块（深度报告工作台）

#### 2.2.1 前端功能

1. **报告生成触发机制（双轨制）**：
* **对话/指令输入框**：支持自然语言意图识别（NLP），如输入“*生成张总下半年的稳健型配置报告*”，按回车直接触发。
* **向导式按钮触发 (GUI)**：提供结构化的“用户选择 -> 偏好 Tag 勾选 -> 点击‘生成资产配置报告’按钮”的标准操作流。


2. **沉浸式加载态感知 (Step-Loading)**：
* 由于报告生成通常耗时十秒以上，需摒弃死板的转圈 Loading。采用**分步式骨架屏与进度节点**（如：`正在提取画像... -> 正在调用onerec匹配资产... -> AI深度撰写逻辑中...`），大幅缓解用户等待焦虑。


3. **富媒体报告渲染展示 (Report Viewer)**：
* **格式化渲染引擎**：支持后端返回的长篇 Markdown 内容解析（含标题、加粗、无序列表）。
* **动态图表与表格注入**：自动提取 JSON 结构化数据，动态渲染大类资产配置饼图、历史回测折线图（ECharts）及标准的财务数据比对表格。


4. **资产化输出与分发**：
* **PDF导出**：前端一键将完整报告 DOM 转化为带有机构品牌水印与合规页脚的 PDF 文件。
* **分享功能**：生成带有时效性与访问密码的短链接或加密邮件分享机制。



#### 2.2.2 后端功能

1. **`onerec` 框架适配器模块 (Anti-Corruption Layer)**：
* **标准化接口抽象**：建立防腐层，无论底层 `onerec` 逻辑如何演进，向上层提供的始终是标准化的产品推荐 JSON 数组。
* **生命周期预留**：开发期注入 Mock 数据；预留生产环境的 HTTP/RPC 真实对接点。具备超时重试与熔断降级能力。


2. **资产配置报告生成服务 (LLM Pipeline)**：
* **可定制 Prompt 模板引擎**：维护多种理财师人设的 Prompt 库。运行时动态组装【用户画像特征】与【onerec召回产品】，注入模板作为大模型输入。
* **防重缓存机制 (Redis)**：基于 `Hash(用户ID + 画像特征 + 日期)` 生成唯一 Key。短时间内相同的请求直接命中 Redis 缓存返回，节约算力成本与 Token 开销。



### 2.3 交互式推荐模块（AI 动态助手）

#### 2.3.1 页面设计

* **金融级气泡流布局**：深度参考“AI涨乐”界面，采用时间倒序的对话流。引入“红涨绿跌”语义化色彩规范，数字强制等宽对齐。
* **防遮挡输入区**：底部输入框吸底固定，随多行文本自适应增高，内置快捷指令推荐（Prompt Chips）。

#### 2.3.2 核心功能 (Generative UI)

1. **流式问答与思考路径透出 (Chain of Thought - CoT)**：
* 极速打字机上屏体验，尾部跟随闪烁光标。
* 在给出具体产品前，以**折叠面板 (Accordion)** 的形式展示大模型的“思考链”（如：*正在分析当前行情... 筛选高股息板块...*），增强专业可信度。


2. **生成式推荐结果展示 (动态微卡片)**：
* 摒弃纯文本限制。当 AI 推荐资产时，前端截获特定指令，实时将其渲染为“产品微前端卡片”（包含名称、代码、净值走势微缩图、核心推荐理由）。


3. **信息渐进式披露 (展开/收起)**：
* 针对卡片，支持“点击展开详情”查看深度指标与研报摘要，优化屏幕纵向空间。


4. **推荐方案对比台 (Compare Desk)**：
* 用户在对话流中可勾选多只推荐产品，点击“对比”后，从右侧（或底部）滑出抽屉，以雷达图和多维表格横向比对多资产方案。



---

## 三、技术架构设计

### 3.1 前端技术架构

前端采用 **SPA + 协议驱动 UI (Schema-Driven UI)** 理念。

1. **技术栈选择**
* **核心框架**：React.js 18+ (利用 Concurrent Mode 优化大量图表渲染卡顿)。
* **开发语言**：TypeScript (严格类型校验，防止金融字段缺失导致白屏)。
* **状态管理**：Zustand (轻量级，比 Redux/Context 更适合处理高频的 SSE 打字机游标更新)。
* **UI/图表库**：Ant Design v5 + Apache ECharts (`echarts-for-react`)。
* **网络客户端**：Axios (常规 REST 请求) + 原生 Fetch API (处理大模型 SSE 流式推送)。


2. **架构分层**
* 表现层：`WorkspaceTabs` (页签切换)、`ChatBubble` (消息气泡)、`ReportViewer` (报告视图)。
* 业务逻辑层：`GenerativeParserEngine` (大模型流式混合协议解析引擎)。
* 数据层：`useChatStore` (会话状态)、`useReportStore` (异步报告状态)。


3. **关键模块设计：虚拟列表性能优化 (Virtual List)**
* 在交互式对话中，当对话轮数过多时，仅渲染视口内的消息气泡与图表实例，**强行销毁视口外的 Canvas 实例**，彻底解决浏览器端内存泄漏 (OOM)。



### 3.2 后端技术架构

后端作为**大模型网关与业务编排层 (BFF)**。

1. **技术栈选择**
* **基础环境**：Node.js (NestJS 框架，对 TS 和前端极度亲和) 或 Java (Spring Boot 3.x)。
* **LLM 集成层**：LangChain (标准化封装对接不同大语言模型)。
* **缓存与消息队列**：Redis (会话上下文) + BullMQ/RabbitMQ (异步处理耗时报告任务)。


2. **服务架构分层**
* **API 网关层**：鉴权、限流、协议转换 (HTTP / SSE)。
* **业务编排服务层**：处理对话拆解、用户鉴权、调用底层服务。
* **领域服务层 (核心)**：`onerec` Adapter (清洗转换逻辑)、LLM Prompt Template Manager。


3. **核心模块设计：异步长任务机制**
* 针对生成式长报告，采用异步化调度。避免前台 HTTP 请求长时间 pending 导致 Nginx 超时截断。



### 3.3 接口设计契约 (API Schema)

1. **生成式推荐相关接口 (RESTful + 异步轮询)**
* `GET /api/v1/users/profiles`：获取可选用户画像列表。
* `GET /api/v1/onerec/adapter/products?userId={id}`：【内部透传】获取 `onerec` 适配后的标准化产品。
* `POST /api/v1/report/generate`：触发生成任务，将任务推入队列，**返回 `{"taskId": "T-123"}**`。
* `GET /api/v1/report/status?taskId=T-123`：前端轮询获取进度，生成完毕返回完整的 Markdown+JSON 结构数据。


2. **交互式推荐相关接口 (Server-Sent Events)**
* `POST /api/v1/chat/completions`
* 配置 `Headers: { 'Accept': 'text/event-stream' }`。
* 后端流式返回 Chunks：
* `data: {"type": "thinking", "content": "正在为您寻找资产..."}`
* `data: {"type": "text", "content": "为您推荐以下标的："}`
* `data: {"type": "widget", "widgetName": "FundCard", "data": {"code": "000001"}}`







---

## 四、实施与测试要求

1. **开发环境配置**
* 前端：Node 18+，使用 `pnpm` 包管理，`Vite` 构建引擎（极速冷启动）。
* 后端：Node 18+ 或 JDK 17+。


2. **严苛的测试要求**
* **单元测试 (Unit Test)**：对前端的“流式协议解析器”与后端的“onerec适配器清洗逻辑”编写 Jest 测试，覆盖率强行要求 **≥ 80%**。
* **集成测试**：重点测试大模型 API 超时、`onerec` 接口宕机等极端情况下的容错降级表现。
* **性能指标 (Performance)**：生成式报告渲染首屏 (LCP) < 1.5s；交互式问答**首字响应时间 (TTFB)** < 1.0s。


3. **交付物清单**
* 本需求与架构设计文档。
* 前后端全量源代码及构建/部署配置（Dockerfile）。
* Swagger / OpenAPI 3.0 格式的标准 API 接口文档。
* 性能压力测试及自动化测试覆盖率报告。



---

## 五、项目约束与安全合规注意事项

1. **`onerec` 边界约束与防幻觉机制**
大语言模型存在捏造数据的“幻觉”风险。后端必须进行强制约束：**大模型最终输出的资产代码，必须严格存在于 `onerec` 适配器召回的候选池中**。若模型自造资产，后端层需进行前置拦截与纠正。
2. **前端质量红线**
前端实现为本项目门面。必须保证 UI 像素级还原、图表加载平滑无闪烁（CLS优化）、流式打字机效果不卡顿。
3. **用户数据安全隐私保护**
传入大模型的所有上下文，后端必须进行 **PII（个人身份信息）脱敏**，如将“张三(身份证号xxx)”替换为“客户A”，严禁真实客史明文数据流出内网。
4. **强制免责留痕 (合规底线)**
* **水印注入**：前端渲染底层需强制注入合规声明：“*AI辅助生成内容仅供参考，历史业绩不代表未来表现，不构成实质投资建议*”，且不可被用户隐藏。
* **本地风控**：前端交互输入框须内置违禁词库，在点击发送前直接本地阻断恶意指令。
